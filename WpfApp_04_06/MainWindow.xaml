<Window x:Class="WpfApp_04_06.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp_04_06"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <StackPanel Margin="20">
            <StackPanel Orientation="Horizontal" Margin="10">
                <TextBox Name="ProcessPathTextBox" Width="250" Margin="0,0,10,0" />
                <Button Content="Launch Process" Click="LaunchProcess_Click"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" Margin="10">
                <Button Name="WaitButton" Content="Wait for Exit" Click="WaitForExit_Click" IsEnabled="False" Margin="0,0,10,0"/>
                <Button Name="KillButton" Content="Kill Process" Click="KillProcess_Click" IsEnabled="False"/>
            </StackPanel>
            <TextBlock Name="StatusTextBlock" Text="Enter the path to the application and click 'Launch Process'" Margin="10"/>
        </StackPanel>
    </Grid>
</Window>
